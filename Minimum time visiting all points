class Solution:
    def minTimeToVisitAllPoints(self, points: List[List[int]]) -> int:
        total_time = 0
        for i in range(1, len(points)):
            x1, y1 = points[i-1]
            x2, y2 = points[i]
            dx = abs(x2 - x1)
            dy = abs(y2 - y1)
            # The minimum time is the Chebyshev distance
            total_time += max(dx, dy)
        return total_time
5. 1281. Subtract the Product and Sum of Digits of an Integer
class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        product = 1
        total_sum = 0
        # Process each digit
        while n > 0:
            digit = n % 10  # Get last digit
            product *= digit
            total_sum += digit
            n //= 10  # Remove last digit
        return product - total_sum
